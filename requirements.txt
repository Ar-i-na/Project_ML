# Все зависимости проекта: EDA, TF-IDF-baseline (tfidf_logreg,
# tfidf_linearsvc), альтернативный подход (tfidf_char_svm), метрики и
# анализ ошибок. Никаких компиляторов/системных библиотек не требуется.
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
